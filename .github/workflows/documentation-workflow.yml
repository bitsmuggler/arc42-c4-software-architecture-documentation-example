name: build-software-architecture-documentation
run-name: Build Software Architecture Documentation
on: [push]
jobs:
    compile:
        name: Build documentation
        runs-on: ubuntu-latest
        steps:
          - name: Check out the repo
            uses: actions/checkout@v2
          - name: Build documentation
            uses: addnab/docker-run-action@v3
            with:
                image: asciidoctor/docker-asciidoctor:latest
                options: -v ${{ github.workspace }}:/documents/
                run: |
                    curl --show-error --location https://static.structurizr.com/download/structurizr-cli.zip | tar -xf - -C /documentsarc42-c4-software-architecture-documentation-example/bin/structurizr-cli
                    /documents/arc42-c4-software-architecture-documentation-example/bin/structurizr-cli/structurizr.sh export -w bank.dsl -format plantuml/structurizr -output diagrams
                    asciidoctor -b html5 -r asciidoctor-diagram documentation/internet-banking-system.adoc